--
-- ORDERS table
--
CREATE TABLE ORDERS (
  ORDER_ID  INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
  SHIP_DATE  TIMESTAMP  NOT NULL,
  STATUS  VARCHAR(10) NOT NULL,
  QUANTITY INTEGER NOT NULL,
  PET_ID  INTEGER
);

ALTER TABLE ORDERS ADD FOREIGN KEY (PET_ID) REFERENCES PET(PET_ID);

-- DUMMY DATA

INSERT INTO ORDERS (ORDER_ID, SHIP_DATE, STATUS, PET_ID, QUANTITY) VALUES (1, TO_DATE('2018-11-11 12:12:12', 'YYYY-MM-DD HH:MI:SS'), 'PLACED', 3, 5);
INSERT INTO ORDERS (ORDER_ID, SHIP_DATE, STATUS, PET_ID, QUANTITY) VALUES (2, TO_DATE('2018-11-11 09:34:44', 'YYYY-MM-DD HH:MI:SS'), 'APPROVED', 4, 2);